{
  "name": "frontend",
  "private": true,
  "type": "module",

  "_comment": "React SPA with Monaco Editor. Deployed to Vercel via vercel.json at monorepo root",

  "scripts": {
    "_comment_dev": "Vite dev server with HMR on port 5173",
    "dev": "vite",

    "_comment_build": "Type-check with tsc (no output), then bundle with Vite → dist/",
    "build": "tsc -p tsconfig.json --noEmit && vite build",

    "preview": "vite preview"
  },

  "dependencies": {
    "@monaco-editor/react": "^4.7.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.3",
    "vite": "^6.1.0"
  }
}

