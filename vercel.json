{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "_comment_1": "This file configures Vercel to build ONLY the frontend from this monorepo.",
  "_comment_2": "Vercel connects to your GitHub repo and auto-deploys on every push to main.",

  "buildCommand": "npm run build:frontend",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install",
  "framework": "vite",

  "rewrites": [
    {
      "_comment": "SPA catch-all: sends all non-file requests to index.html for client-side routing",
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
